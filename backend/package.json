{
  "name": "rac-hub-submit-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend service for RAC Hub Submit — validates PINs, accepts submissions, writes to Google Drive, manages approvals.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "googleapis": "^171.4.0",
    "multer": "^2.1.1",
    "resend": "^4.0.0",
    "web-push": "^3.6.7"
  }
}
